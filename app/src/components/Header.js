import React, { useEffect } from "react";
import { connect } from "react-redux";

const Header = (props) => {
    return (
        <header>

        </header>
    )
}



export default connect(null, {  })(Header);